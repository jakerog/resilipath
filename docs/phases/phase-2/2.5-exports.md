# Sub-Phase Tasklist: Advanced Exports (DOCX/XLSX) (v1.0)

**Project:** ResiliPath
**Phase:** 2
**Domain:** Portability & Compliance
**Status:** Initializing

---

Sub-Phase Title: Advanced Exports (DOCX/XLSX)

Primary Concern: Generating portable, physical copies of resilience plans for offline storage.

Dependencies: Phase 2.2 Planning Engine and Phase 2.4 Lifecycle Hooks.

Atomic Task List:

[x] Task 1: Identify and install serverless-friendly NPM packages for DOCX and XLSX generation (e.g., `docx`, `exceljs`).

[x] Task 2: Create `functions/src/planning/exports.ts` with the `generatePlanExport` HTTPS callable.

[x] Task 3: Implement `DOCX` generation logic for the full `ResiliencePlan` (sections, questions, and responses).

[x] Task 4: Implement `XLSX` generation logic for the `AssetRegistry` (critical systems, vendors, and their status).

[x] Task 5: Register the `generatePlanExport` function in `functions/src/index.ts`.

[x] Task 6: Add "Export Plan (DOCX)" and "Export Asset Inventory (XLSX)" buttons to the Plan Editor UI.

[x] Task 7: Ensure exported files follow naming conventions: `ResiliPath_Plan_[PlanName]_[ISO-Date].docx`.
